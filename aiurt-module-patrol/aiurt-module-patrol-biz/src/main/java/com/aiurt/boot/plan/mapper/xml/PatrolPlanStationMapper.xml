<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.boot.plan.mapper.PatrolPlanStationMapper">

    <select id="selectStationByPlanCode" resultType="com.aiurt.boot.plan.param.PatrolPlanStationParam">
        SELECT cs.station_name,
        pps.*
        FROM patrol_plan_station pps
        JOIN cs_station cs ON pps.station_code = cs.station_code
        where pps.del_flag = 0
        <if test="planCode != null and planCode != ''">
            and pps.plan_code=#{planCode}
        </if>
    </select>

    <select id="getPlanCodeByUserStation" resultType="java.lang.String">
        select distinct plan_code
        from patrol_plan_station
        where del_flag = 0
    </select>
</mapper>
