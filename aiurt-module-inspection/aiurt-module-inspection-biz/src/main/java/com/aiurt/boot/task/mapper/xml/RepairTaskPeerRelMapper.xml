<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.boot.task.mapper.RepairTaskPeerRelMapper">

    <select id="selectTaskIdWithPeerNames" resultType="com.aiurt.boot.task.dto.RepairTaskPeerNameDTO">
        SELECT
        t1.id,
        (SELECT group_concat(DISTINCT t3.real_name) FROM repair_task_peer_rel AS t3 WHERE t2.`code` = t3.repair_task_device_code AND t3.del_flag = 0) AS peerNames
        FROM
        repair_task t1
        LEFT JOIN repair_task_device_rel t2 ON t1.id = t2.repair_task_id
        WHERE
        t1.del_flag = 0
        AND t2.del_flag = 0
        <if test="list != null and list.size() > 0">
            AND t1.id IN
            <foreach item="id" index="index" collection="list" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
        GROUP BY
        t1.id
    </select>
</mapper>