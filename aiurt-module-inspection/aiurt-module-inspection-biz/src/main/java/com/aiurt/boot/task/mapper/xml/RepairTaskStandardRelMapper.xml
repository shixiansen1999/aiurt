<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.boot.task.mapper.RepairTaskStandardRelMapper">

    <select id="getRepairTaskCode" resultType="java.lang.String">
    SELECT
        DISTINCT rt.code
    FROM
	repair_task_standard_rel
	RIGHT JOIN repair_task rt ON rt.id = repair_task_standard_rel.repair_task_id
    </select>
</mapper>
