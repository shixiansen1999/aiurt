<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.boot.weeklyplan.mapper.BdSparesListMapper">
    <select id="getMessage" resultType="string">
     SELECT id from sys_announcement where announcement_type_id = #{messageType} and bus_id =#{id} and del_flag = 0
    </select>
    <update id="updateMessage" parameterType="string">
     update sys_announcement_send set read_flag=1   where annt_id = #{id}
    </update>
</mapper>
