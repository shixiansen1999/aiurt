<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.modules.online.page.mapper.ActCustomPageModuleMapper">

	<update id="updateTreeNodeStatus" parameterType="java.lang.String">
		update act_custom_page_module set has_child = #{status} where id = #{id}
	</update>

  	<!-- 【vue3专用】 -->
	<select id="queryListByPid" parameterType="java.lang.Object" resultType="org.jeecg.common.system.vo.SelectTreeModel">
		select
		  id as "key",
		  module_name as "title",
		  (case when has_child = '1' then 0 else 1 end) as isLeaf,
		  pid as parentId
		from act_custom_page_module
		where pid = #{pid}
		<if test="query != null">
			<foreach collection="query.entrySet()" item="value" index="key">
				and ${key} = #{value}
			</foreach>
		</if>
	</select>

	<select id="getModuleTree" parameterType="java.lang.Object" resultType="org.jeecg.common.system.vo.SelectTreeModel">
		SELECT
		id AS `key`,
		module_name AS title,
		id AS `value`,
		(case when has_child = '1' then 0 else 1 end) as isLeaf,
		IFNULL( pid, 0 ) AS parentId
		FROM
		act_custom_page_module
		<where>
			<if test="name != null and name != ''">
				and module_name LIKE CONCAT('%', #{name}, '%')
			</if>
		</where>
	</select>

</mapper>
