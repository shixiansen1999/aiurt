<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.modules.schedule.mapper.ScheduleMapper">

<select id="getUser" resultType="org.jeecg.common.system.vo.LoginUser">
    select id,org_id from sys_user where del_flag = 0 and realname = #{realName} and work_no =#{workNo}
    </select>

<select id="getDepartByName" resultType="org.jeecg.common.system.vo.SysDepartModel">
    select id,parent_id,depart_name from sys_depart where del_flag = 0 and depart_name = #{departName}
    </select>
    <select id="queryUserForNightCount" resultType="com.aiurt.modules.schedule.vo.ScheduleRecordVo">
        select id userId, realname username, org_name department
        from sys_user
        where del_flag = 0
        <if test="orgId != null and orgId != ''">
            and org_id = #{orgId}
        </if>
        <if test="userName != null and userName != ''">
            and realname like concat('%', #{userName}, '%')
        </if>
        <if test="exOrgId != null and exOrgId != ''">
            and org_id != #{exOrgId}
        </if>
    </select>
</mapper>
