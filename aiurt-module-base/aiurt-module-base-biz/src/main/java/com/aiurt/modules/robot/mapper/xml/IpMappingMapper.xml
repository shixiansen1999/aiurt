<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.modules.robot.mapper.IpMappingMapper">

    <select id="duplicateCheckCount" resultType="java.lang.Long">
     SELECT COUNT(*) FROM ip_mapping WHERE outside_ip = #{condition.outsideIp} and outside_port =#{condition.outsidePort} and id &lt;&gt; #{condition.id}
    </select>
    <select id="duplicateCheckCountNoId" resultType="java.lang.Long">
     SELECT COUNT(*) FROM ip_mapping WHERE outside_ip = #{condition.outsideIp} and outside_port =#{condition.outsidePort}
    </select>
</mapper>
