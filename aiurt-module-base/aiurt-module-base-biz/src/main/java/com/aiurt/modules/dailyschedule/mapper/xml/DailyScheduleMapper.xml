<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.modules.dailyschedule.mapper.DailyScheduleMapper">

    <select id="queryDailyScheduleList" resultType="com.aiurt.modules.dailyschedule.entity.DailySchedule">
        select a.*, DATE_FORMAT(a.add_time, '%Y/%m/%d') as addTimeAlias
        FROM  daily_schedule a
        where 1=1
        <if test="year!=null">
           and DATE_FORMAT(a.add_time, '%Y') = #{year}
        </if>
        <if test="month != null">
            and DATE_FORMAT(a.add_time, '%c') = #{month}
        </if>
        <if test="day != null ">
            and DATE_FORMAT(a.add_time, '%e') = #{day}
        </if>

        <if test=" userId != null and userId != ''">
            and  (
               find_in_set(#{userId}, a.notify_user_id)>0
            )
        </if>
        <if test="addTime != null">
            and add_time = #{addTime}
        </if>
    </select>
</mapper>
