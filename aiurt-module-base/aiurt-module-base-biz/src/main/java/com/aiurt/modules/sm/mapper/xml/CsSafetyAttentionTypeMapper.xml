<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.modules.sm.mapper.CsSafetyAttentionTypeMapper">

	<update id="updateTreeNodeStatus" parameterType="java.lang.String">
		update cs_safety_attention_type set has_child = #{status} where id = #{id}
	</update>

  	<!-- 【vue3专用】 -->
	<select id="queryListByPid" parameterType="java.lang.Object" resultType="org.jeecg.common.system.vo.SelectTreeModel">
		select
		  id as "key",
		  name as "title",
		  (case when has_child = '1' then 0 else 1 end) as isLeaf,
		  pid as parentId
		from cs_safety_attention_type
		where pid = #{pid}
		<if test="query != null">
			<foreach collection="query.entrySet()" item="value" index="key">
				and ${key} = #{value}
			</foreach>
		</if>
	</select>
    <select id="queryTreeList" resultType="com.aiurt.common.api.vo.TreeNode">
    select id as value,id,name,name as label,pid,major_code as extendedField
    from cs_safety_attention_type where del_flag = 0 order by sort desc
	</select>
	<select id="queryAllMajor" resultType="com.aiurt.common.api.vo.TreeNode">
		select major_code as value,major_code as id,major_name as name,major_name as label from cs_major where del_flag = 0
	</select>

</mapper>
