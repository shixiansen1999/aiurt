<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.modules.signmeeting.mapper.ConfereeMapper">

	<delete id="deleteByMainId" parameterType="java.lang.String">
		DELETE 
		FROM  conferee 
		WHERE
			 meeting_id = #{mainId} 	</delete>
	
	<select id="selectByMainId" parameterType="java.lang.String" resultType="com.aiurt.modules.signmeeting.entity.Conferee">
		SELECT * 
		FROM  conferee
		WHERE
			 meeting_id = #{mainId} 	</select>
</mapper>
