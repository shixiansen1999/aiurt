<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.modules.system.mapper.CsUserMajorMapper">

<select id="getMajorIds" resultType="java.lang.String">
    select major_id from cs_user_major where user_id = #{userId}
    </select>

<select id="getMajorByUserId" resultType="org.jeecg.common.system.vo.CsUserMajorModel">
    select cum.id ,cum.user_id,cum.major_id,cm.major_code ,cm.major_name from cs_user_major cum left join cs_major cm on cm.id = cum.major_id where cum.user_id = #{id}
</select>
</mapper>