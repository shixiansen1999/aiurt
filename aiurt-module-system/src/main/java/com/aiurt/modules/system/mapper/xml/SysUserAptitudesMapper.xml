<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiurt.modules.system.mapper.SysUserAptitudesMapper">
    <select id="getAptitude" resultType="org.jeecg.common.system.vo.RadarAptitudeModel">
        select sua.user_id     userId,
               sum(sua.number) number
        from sys_user_aptitudes sua
                 join sys_user su on (sua.user_id = su.id and sua.del_flag = 0)
        where sua.del_flag = 0
          and su.org_code = #{orgCode}
        group by sua.user_id
    </select>
</mapper>
